{% extends 'base.html.twig' %}

{% block title %}Movie{% endblock %}

{% block body %}
<main class="container-fluid">
 <div class="row">
      <div class="card col-sm-12 text-center " >
         <img src="{{asset('build/images/doctor_strange.jpg')}}" class="rounded image-fluid  mt-3 show_movie_image " alt="...">
         <div class="card-body">
           <h5 class="card-title">{{movie.title}}</h5>
           <p class="card-text">{{movie.description}}</p>
         </div>
         <ul class="list-group list-group-flush">
           <li class="list-group-item">An item</li>
           <li class="list-group-item">A second item</li>
           <li class="list-group-item">A third item</li>
         </ul>
         <div class="card-body">
            <div class="d-grid gap-2 d-md-flex justify-content-md-center text-center">
               <div class="btn btn-primary me-md-2 ">
               <a href="{{path('app_movie_edit',{'id': movie.id})}}" class="btn btn-primary me-md-2 " >Edit</a>
               </div>
               <div class="btn btn-danger me-md-2>
                  {%include('movie/_delete_form.html.twig')%}
               </div>
             </div>
         </div>
       </div>
    </div>
   









</main>
{% endblock %}
